export enum Sizes {
  'body' = 16,
  'headline' = 48,
  'question' = 20,
  'pagination' = 16,
}

export enum Weights {
  'body' = '300',
  'headline' = '600',
  'question' = '300',
  'pagination' = '200',
}
