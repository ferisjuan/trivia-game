import colors from "../constants/colors";

export enum Sizes {
  'body' = 16,
  'headline' = 30,
  'question' = 20,
  'pagination' = 16,
}

export enum Weights {
  'body' = '300',
  'headline' = 'bold',
  'question' = '300',
  'pagination' = '200',
}

